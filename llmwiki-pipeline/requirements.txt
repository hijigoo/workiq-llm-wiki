# Core
python-dotenv>=1.0.1

# MCP client (Streamable HTTP transport)
mcp>=1.12.0

# Microsoft Entra ID auth (delegated device-code + app-only client-credentials)
msal>=1.31.0

# LLM (OpenAI + Azure OpenAI); Azure keyless auth uses azure-identity
openai>=1.51.0
azure-identity>=1.19.0

# Web app
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
itsdangerous>=2.2.0  # required by Starlette SessionMiddleware (browser login sessions)

# Notebooks
jupyter>=1.1.1
ipykernel>=6.29.0
